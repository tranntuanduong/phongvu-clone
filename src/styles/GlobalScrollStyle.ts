import { createGlobalStyle } from 'styled-components';

//5*
const GlobalScrollStyle = createGlobalStyle`
  body {
    overflow-y: hidden;
    padding-right: 17px;
  }
`;

export default GlobalScrollStyle;
